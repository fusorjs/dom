{
  "name": "@perform/dom",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "Igor Sukharev <imixaly4@gmail.com>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "test": "jest src",
    "test-coverage": "jest src --coverage",
    "start": "shx rm -rf dist/* && tsc -p tsconfig.dist.json --watch",
    "prepublishOnly": "shx rm -rf dist/* && tsc -p tsconfig.dist.json"
  },
  "peerDependencies": {
    "@perform/common": "link:../common/dist"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "shx": "^0.3.3",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.5"
  }
}
